% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cklut.R
\name{cklut_to_csv}
\alias{cklut_to_csv}
\title{Export a cklut table to CSV}
\usage{
cklut_to_csv(ck, path, ...)
}
\arguments{
\item{ck}{A \code{cklut} handle or path to a \code{.ckmeta} file.}

\item{path}{Output \code{.csv} path.}

\item{...}{Passed to \code{data.table::fwrite}.}
}
\value{
Invisibly, \code{path}.
}
\description{
Writes the full table (keys + values) to CSV using \code{data.table::fwrite}.
The output round-trips back through \code{\link{cklut_build}}.
}
\examples{
library(data.table)
dt <- CJ(year = 2015:2018, sex = factor(c("men", "women")))
dt[, value := year + (sex == "men")]
base <- tempfile("cklut")
ck <- cklut_build(dt, base, keys = c("year", "sex"))

# export the full table to a CSV file
csv <- tempfile(fileext = ".csv")
cklut_to_csv(ck, csv)
file.exists(csv)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{hc_effect}
\alias{hc_effect}
\title{Health Care Effect with Continuation Probability}
\usage{
hc_effect(x, prb_of_continuation, pid)
}
\arguments{
\item{x}{An integer vector representing health care effect status (1 = present, 0 = absent).
Must be sorted by year/time within person.}

\item{prb_of_continuation}{A numeric value representing the probability of effect continuation}

\item{pid}{A logical vector marking new person IDs (TRUE for new person, FALSE otherwise).
Data must be grouped by person and sorted by year/time.}
}
\value{
An integer vector with updated health care effect status
}
\description{
Models the continuation of a health care effect based on a probability of continuation.
When the previous time step had the effect (value 1) and it's not a new person,
a binomial trial determines if the effect continues.
}
\details{
**Important**: The input data must be grouped by person ID and sorted by year/time within each person.
This ensures proper temporal sequencing for modelling effect continuation.
}
\examples{
# Data grouped by person id and sorted by time within person
pid <- c(1L, 1L, 1L, 2L, 2L, 3L)
mrk <- mk_new_simulant_markers(pid)
x   <- c(1L, 0L, 0L, 1L, 0L, 1L)

# With probability 1 the effect always continues within a person
set.seed(1)
hc_effect(x, prb_of_continuation = 1.0, pid = mrk)  # 1 1 1 1 1 1
}
